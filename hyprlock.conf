$color = rgba(15, 12, 35, 0.95)           # Fundo base
$inner_color = rgba(15, 12, 35, 0.65)     # Fundo campo input
$outer_color = rgba(180, 170, 220, 0.9)   # Borda e destaques
$font_color = rgba(220, 210, 240, 1.0)      # Cor de texto principal
$check_color = rgba(140, 80, 200, 1.0)      # Sucesso/autenticação

# Extra
$fail_color   = rgba(255, 85, 85, 1.0)      # Erro
$caps_color   = rgba(241, 250, 140, 1.0)    # Caps Lock ativo